---
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faFacebook } from "@fortawesome/free-brands-svg-icons";
import { faInstagram } from "@fortawesome/free-brands-svg-icons";
import { faYoutube } from "@fortawesome/free-brands-svg-icons";
type Props = {
    classes: string;
    styles: string;
    service: string;
    url: string;
};

const {classes, styles, service, url} = Astro.props;
---
<div class={classes} style={styles}>
    {service === "facebook" && <a href={url} target="_blank" class="text-3xl hover:text-blue-500">
        <FontAwesomeIcon icon={faFacebook}/>
    </a>  }
    {service === "instagram" && <a href={url} target="_blank"  target="_blank" class="text-3xl hover:text-pink-500">
       <FontAwesomeIcon icon={faInstagram} />
         </a> }
    {service === "youtube" && <a href={url} target="_blank"  target="_blank" class="text-3xl hover:text-red-500">
     <FontAwesomeIcon icon={faYoutube} />
        </a>
    }
</div>