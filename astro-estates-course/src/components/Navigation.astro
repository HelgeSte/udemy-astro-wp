---
type Props = {
    styles: string;
    classes: string;
};
const {styles, classes} = Astro.props;
---
<nav>
    <ul class={classes} style={styles}>
        <slot /> {/*blockrenderedinner will be rendered here */}
    </ul>
</nav>